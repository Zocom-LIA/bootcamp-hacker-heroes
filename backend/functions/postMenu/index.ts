import { sendResponse } from '../../responses';
import {db} from '../../services/index'


async function postMenu(body:Array<Object>) {
    
}

exports.handler = async () => {
    
    return sendResponse(200, { success: true });
}